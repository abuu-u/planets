{
  "name": "planets",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && xo --fix && rollup -c",
    "start": "npm run build && node dist/app.js"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^13.0.6",
    "@rollup/plugin-typescript": "^8.3.0",
    "@types/node": "^16.11.9",
    "rimraf": "^3.0.2",
    "rollup": "^2.60.0",
    "rollup-plugin-terser": "^7.0.2",
    "tslib": "^2.3.1",
    "typescript": "^4.5.2",
    "xo": "^0.46.4"
  }
}
